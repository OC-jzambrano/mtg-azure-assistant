[pytest]
markers =
    integration: Tests that interact with live external services (deselect with '-m "not integration"')
addopts = -m "not integration" -v
testpaths = tests
asyncio_mode = strict
pythonpath = .

